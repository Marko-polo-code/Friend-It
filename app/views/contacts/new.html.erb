 <div class="container">
  

  <%= simple_form_for([@flat, @contact]) do |f| %>
    <%= f.input :phone_number %>
    <%= f.input :address %>
    <%= f.input :description %>
    <%= f.submit %>
  <% end %>

</div>

