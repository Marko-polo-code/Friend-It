 <div class="container">
  

  <%= simple_form_for([@flat, @recommendation]) do |f| %>
    <%= f.input :name %>
    <%= f.input :description %>
    <%= f.input :address %>
    <%= f.submit %>
  <% end %>

</div>